// eslint-disable-next-line
import React from 'react';
import {atom} from 'recoil';

const AroundSubwayState = atom({
    key: 'aroundSubwayState',
    default: [],
});

export default AroundSubwayState;